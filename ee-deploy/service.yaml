apiVersion: v1
kind: Service
metadata:
  name: seamstress-design-pr-${PR_NUMBER}
  labels:
    app: seamstress-design
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: seamstress-design

